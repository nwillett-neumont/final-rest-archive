// The book object structure used throughout the library API.
// A book has a title (string), a status (string), and a rating (number).
export interface Book {
  title: string;
  status: string;
  rating: number;
}
